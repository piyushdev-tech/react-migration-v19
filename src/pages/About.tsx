import Container from 'react-bootstrap/Container'

export function About() {
  return (
    <Container>
      <h1>About</h1>
      <p>
        This project scaffolds React 18.3.0 with Vite, TypeScript, Vitest, React Testing
        Library, Zustand, React Query, ag-grid, react-bootstrap, and react-router — the
        baseline this repo's <code>SKILL.md</code> and <code>instructions.md</code> use when
        migrating to React 19.2.7.
      </p>
    </Container>
  )
}
