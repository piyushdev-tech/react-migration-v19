import Container from 'react-bootstrap/Container'

export function About() {
  return (
    <Container>
      <h1>About</h1>
      <p>
        This project scaffolds React 16.14.0 with Vite, TypeScript, Vitest, React Testing
        Library, Zustand, React Query, ag-grid, react-bootstrap, and react-router — the
        baseline this repo's <code>SKILL.md</code> and <code>instructions.md</code> use to
        test one hop at a time (16→17 first) toward React 19.
      </p>
    </Container>
  )
}
